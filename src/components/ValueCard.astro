---
import Card from "./Card.astro";

interface Props {
  title: string;
  description: string;
}

const { title, description } = Astro.props;
---

<Card className="h-full p-6">
  <h3 class="text-[1.3rem] leading-[1.15] text-accent-contrast">{title}</h3>
  <p class="mt-3">{description}</p>
</Card>
